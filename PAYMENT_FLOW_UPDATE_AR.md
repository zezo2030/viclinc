# تحديث فلو الدفع في الويبسايت

## نظرة عامة
تم تحديث فلو حجز الموعد ليشمل عملية الدفع بشكل كامل ومتكامل. الآن عند حجز موعد يتطلب دفعاً (VIDEO/CHAT)، يتم توجيه المستخدم تلقائياً إلى صفحة الدفع.

---

## 📋 التغييرات المنفذة

### 1. إنشاء API Service للدفع
**الملف:** `viclinc/websit/src/lib/api/payments.ts`

تم إنشاء service جديد يتضمن:
- `createPaymentIntent()` - إنشاء نية دفع
- `getPaymentByAppointment()` - جلب حالة الدفع للموعد
- `verifyPayment()` - التحقق من حالة الدفع

### 2. تحديث أنواع البيانات
**الملف:** `viclinc/websit/src/lib/api/appointments.ts`

تم تحديث `Appointment` interface لإضافة:
- `requiresPayment?: boolean`
- `paymentStatus?: 'NONE' | 'PENDING' | 'COMPLETED'`
- `price?: number`
- `startAt?: string` و `endAt?: string` (للـ API الجديد)
- دعم `id` كـ `string | number` (MongoDB ObjectId أو SQL ID)

### 3. تحديث صفحة الحجز
**الملف:** `viclinc/websit/src/app/appointments/new/page.tsx`

**التغييرات:**
- بعد إنشاء الموعد، يتم فحص `requiresPayment` و `paymentStatus`
- إذا كان `requiresPayment === true` و `paymentStatus === 'PENDING'`:
  - إعادة التوجيه إلى `/appointments/[id]/payment`
- إذا لم يتطلب دفعاً:
  - إعادة التوجيه إلى `/appointments/confirmation?id=[id]`

### 4. إنشاء صفحة الدفع الجديدة
**الملف:** `viclinc/websit/src/app/appointments/[id]/payment/page.tsx`

**المميزات:**
- عرض تفاصيل الموعد
- عرض معلومات الدفع (المبلغ، الحالة)
- إنشاء Payment Intent عند الضغط على "الدفع الآن"
- فتح صفحة Paylink في نافذة جديدة
- التحقق التلقائي من حالة الدفع كل 3 ثوانٍ
- إعادة التوجيه التلقائي إلى صفحة التأكيد عند اكتمال الدفع
- عرض رسائل حالة واضحة (نجح، فشل، قيد الانتظار)
- زر للتحقق اليدوي من حالة الدفع

### 5. تحديث صفحة التأكيد
**الملف:** `viclinc/websit/src/app/appointments/confirmation/page.tsx`

**التغييرات:**
- إضافة فحص للدفع عند تحميل الصفحة
- إذا كان الموعد يتطلب دفعاً ولم يتم الدفع:
  - إعادة التوجيه تلقائياً إلى صفحة الدفع
- تحديث `getAppointment()` لدعم `string` ID (MongoDB ObjectId)

### 6. تحديث Backend
**الملفات:**
- `viclinc/clinic-api/src/modules/payments/dto/payment-response.dto.ts`
- `viclinc/clinic-api/src/modules/payments/payments.service.ts`

**التغييرات:**
- إضافة `metadata` إلى `PaymentResponseDto`
- إرجاع `metadata` في `mapToResponse()` ليشمل `paylinkInvoiceUrl`

---

## 🔄 الفلو الكامل المحدث

### المرحلة 1: حجز الموعد
1. المستخدم يختار الطبيب والخدمة والتاريخ والوقت
2. يضغط على "تأكيد الحجز"
3. يتم إنشاء الموعد في Backend

### المرحلة 2: فحص الدفع
بعد إنشاء الموعد:
- **إذا كان `requiresPayment === true` و `paymentStatus === 'PENDING'`:**
  - إعادة التوجيه إلى `/appointments/[id]/payment`
- **إذا لم يتطلب دفعاً:**
  - إعادة التوجيه إلى `/appointments/confirmation?id=[id]`

### المرحلة 3: صفحة الدفع
1. عرض تفاصيل الموعد والمبلغ
2. المستخدم يضغط على "الدفع الآن"
3. يتم إنشاء Payment Intent في Backend
4. Backend ينشئ فاتورة Paylink ويعيد URL
5. يتم فتح صفحة Paylink في نافذة جديدة
6. المستخدم يدفع في Paylink
7. Paylink يرسل webhook إلى Backend
8. Backend يحدث حالة الدفع
9. Frontend يتحقق من الحالة كل 3 ثوانٍ
10. عند اكتمال الدفع → إعادة التوجيه إلى صفحة التأكيد

### المرحلة 4: صفحة التأكيد
1. التحقق من الدفع (إذا لزم الأمر)
2. عرض تفاصيل الموعد
3. عرض حالة الموعد (مؤكد/قيد المراجعة)

---

## 📝 حالات الدفع

### 1. PENDING (قيد الانتظار)
- الموعد تم إنشاؤه ولكن الدفع لم يكتمل بعد
- يتم عرض زر "الدفع الآن" أو "فتح صفحة الدفع"
- يتم التحقق التلقائي كل 3 ثوانٍ

### 2. COMPLETED (مكتمل)
- تم الدفع بنجاح
- يتم إعادة التوجيه تلقائياً إلى صفحة التأكيد
- يمكن للطبيب تأكيد الموعد

### 3. FAILED (فشل)
- فشل عملية الدفع
- يتم عرض رسالة خطأ
- يمكن للمستخدم المحاولة مرة أخرى

---

## 🔒 نقاط الأمان

1. **التحقق من الملكية:**
   - فقط صاحب الموعد يمكنه الدفع
   - `ProtectedRoute` يضمن تسجيل الدخول

2. **منع الحجز المكرر:**
   - Idempotency Key في Backend
   - التحقق من حالة الدفع قبل إنشاء intent جديد

3. **التحقق من الحالة:**
   - التحقق من أن الموعد موجود
   - التحقق من أن الموعد يتطلب دفعاً
   - التحقق من أن الدفع لم يكتمل مسبقاً

---

## 🧪 الاختبار

### سيناريو 1: حجز موعد حضوري (IN_PERSON)
1. اختيار نوع الموعد: حجز عيادة
2. اختيار الطبيب والخدمة والتاريخ
3. تأكيد الحجز
4. **النتيجة المتوقعة:** إعادة التوجيه مباشرة إلى صفحة التأكيد (لا يتطلب دفعاً)

### سيناريو 2: حجز موعد فيديو (VIDEO)
1. اختيار نوع الموعد: استشارة فيديو
2. اختيار الطبيب والخدمة والتاريخ
3. تأكيد الحجز
4. **النتيجة المتوقعة:** إعادة التوجيه إلى صفحة الدفع
5. الضغط على "الدفع الآن"
6. **النتيجة المتوقعة:** فتح صفحة Paylink
7. إتمام الدفع
8. **النتيجة المتوقعة:** إعادة التوجيه إلى صفحة التأكيد

### سيناريو 3: فشل الدفع
1. حجز موعد فيديو
2. الضغط على "الدفع الآن"
3. فتح صفحة Paylink وإلغاء الدفع
4. **النتيجة المتوقعة:** عرض رسالة "فشل الدفع" مع إمكانية المحاولة مرة أخرى

---

## 📁 الملفات الجديدة والمحدثة

### ملفات جديدة:
- `viclinc/websit/src/lib/api/payments.ts`
- `viclinc/websit/src/app/appointments/[id]/payment/page.tsx`

### ملفات محدثة:
- `viclinc/websit/src/lib/api/appointments.ts`
- `viclinc/websit/src/app/appointments/new/page.tsx`
- `viclinc/websit/src/app/appointments/confirmation/page.tsx`
- `viclinc/clinic-api/src/modules/payments/dto/payment-response.dto.ts`
- `viclinc/clinic-api/src/modules/payments/payments.service.ts`

---

## 🎯 الخطوات التالية (اختياري)

1. **إضافة معالجة أفضل للأخطاء:**
   - عرض رسائل خطأ واضحة
   - إعادة المحاولة التلقائية عند فشل الطلب

2. **تحسين تجربة المستخدم:**
   - إضافة loading states أفضل
   - إضافة animations عند الانتقالات
   - إضافة toast notifications

3. **إضافة ميزات إضافية:**
   - إمكانية إلغاء الدفع
   - عرض تاريخ الدفع
   - إرسال إشعارات عند اكتمال الدفع

---

## ✅ الخلاصة

تم تحديث فلو الدفع بشكل كامل ومتكامل:
- ✅ ربط الدفع بالحجز
- ✅ صفحة دفع مخصصة
- ✅ التحقق التلقائي من الحالة
- ✅ إعادة التوجيه التلقائية
- ✅ معالجة جميع الحالات (نجح، فشل، قيد الانتظار)
- ✅ تحديث Backend لإرجاع metadata

النظام الآن جاهز للاستخدام! 🎉




