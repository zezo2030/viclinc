# دليل صفحة اختبار مكالمات الفيديو

## نظرة عامة

تم إضافة صفحة اختبار لمكالمات الفيديو في الويب تسمح ببدء مكالمات فيديو مباشرة بدون الحاجة لانتظار موعد.

## الموقع

الصفحة متاحة على: `/test/video`

## المميزات

1. **إنشاء موعد تجريبي جديد**: إنشاء موعد فيديو تجريبي وبدء المكالمة مباشرة
2. **استخدام موعد موجود**: استخدام موعد موجود وبدء المكالمة مباشرة
3. **وضع الاختبار التلقائي**: وضع الاختبار مفعّل تلقائياً، مما يتجاوز قيود الوقت (10 دقائق قبل الموعد)

## كيفية الاستخدام

### الطريقة 1: إنشاء موعد تجريبي جديد

1. انتقل إلى `/test/video`
2. اختر "إنشاء موعد تجريبي جديد"
3. اختر طبيب من القائمة
4. اختر خدمة من القائمة
5. اضغط على "إنشاء موعد تجريبي وبدء الاختبار"
6. سيتم إنشاء الموعد ويمكنك بدء المكالمة مباشرة

### الطريقة 2: استخدام موعد موجود

1. انتقل إلى `/test/video`
2. اختر "استخدام موعد موجود"
3. أدخل معرف الموعد (Appointment ID)
4. اضغط على "بدء الاختبار"
5. يمكنك بدء المكالمة مباشرة

## الملفات المضافة/المحدثة

### الملفات الجديدة:
- `websit/src/app/test/video/page.tsx` - صفحة الاختبار

### الملفات المحدثة:
- `websit/src/lib/api/agora.ts` - إضافة دعم وضع الاختبار
- `websit/src/lib/api/client.ts` - إضافة دعم تمرير headers في POST
- `websit/src/components/consultation/VideoCall.tsx` - إضافة prop `testMode`

## كيف يعمل وضع الاختبار

1. عند تفعيل وضع الاختبار (`testMode: true`):
   - يتم إرسال header `x-test-mode: true` مع طلب الحصول على token
   - الـ backend يتجاوز التحقق من الوقت (10 دقائق قبل الموعد)
   - الـ backend يتجاوز الحاجة لتأكيد الموعد (يمكن بدء الجلسة حتى لو كان `PENDING_CONFIRM`)

2. في الـ backend (`video-session.service.ts`):
   ```typescript
   const disableTimeCheck = isTestMode ||
                            process.env.DISABLE_VIDEO_TIME_CHECK === 'true' || 
                            process.env.NODE_ENV === 'development' ||
                            process.env.NODE_ENV === 'test';
   ```

## ملاحظات مهمة

1. **للتطوير والاختبار فقط**: هذه الصفحة مخصصة للاختبار والتطوير
2. **لا تستخدم في الإنتاج**: لا تضع رابط لهذه الصفحة في الإنتاج
3. **الأمان**: تأكد من أن الصفحة محمية بـ `ProtectedRoute` (وهي كذلك)

## المتطلبات

- تسجيل الدخول مطلوب (الصفحة محمية)
- يجب أن يكون لديك حساب في النظام
- يجب أن يكون Agora مُعدّ بشكل صحيح في الـ backend

## اختبار سريع

1. سجل الدخول إلى النظام
2. انتقل إلى `/test/video`
3. اختر طبيب وخدمة
4. اضغط على "إنشاء موعد تجريبي وبدء الاختبار"
5. اضغط على "بدء المكالمة" في مكون الفيديو
6. يجب أن تعمل المكالمة مباشرة ✅

---

**تاريخ الإنشاء**: 2025-01-05  
**الحالة**: ✅ جاهز للاستخدام




